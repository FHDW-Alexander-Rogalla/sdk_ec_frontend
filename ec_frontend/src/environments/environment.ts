export const environment = {
    production: false,
    apiUrl: 'http://localhost:5139/api' /* Connection for when you start the API in HTTP mode */
    // apiUrl: 'https://localhost:7129/api' /* Connection for when you start the API in HTTPS mode */
};